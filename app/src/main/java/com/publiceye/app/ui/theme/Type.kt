package com.publiceye.app.ui.theme

import androidx.compose.material3.Typography

// Using Material Design 3 default typography (Roboto)
// No custom fonts in MVP — per project decision
val Typography = Typography()
